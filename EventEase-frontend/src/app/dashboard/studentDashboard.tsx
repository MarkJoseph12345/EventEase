'use client'

import React, { useState } from 'react';
const StudentDashboard= () => {
  return (
    <div>
      Student Dashboard
    </div>
  );
}
export default /*withAuth  <button onClick={handleLogout}>Log out</button>*/(StudentDashboard);
